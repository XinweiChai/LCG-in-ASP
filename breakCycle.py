def breakCycle(lcg, SCC,startNode)
    for cycle in SCC:
        if len(cycle) == 1:
            continue
       #closest element to the desired state
        if startNode in cycle:
            closest = startNode
        else:
            extPred = #if external predecessor 
        for j in cycle:
            if j == startNode:
                continue
            else:
                pred
    return lcg
