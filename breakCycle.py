def breakCycle(lcg, SCC,startNode)
   for cycle in SCC:
       if len(cycle) == 1:
           continue
       for j in cycle:
           if j == startNode:
               continue
           else:
               pred
    return lcg
